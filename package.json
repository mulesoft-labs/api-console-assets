{
  "name": "api-console-assets",
  "version": "0.1.0",
  "description": "This repo only exists to publish api console components to npm",
  "main": "index.js",
  "scripts": {
    "build": "bower install && find ./bower_components -name package.json -exec rm {} \\;"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mulesoft-labs/api-console-assets.git"
  },
  "author": "Mulesoft",
  "devDependencies": {
    "bower": "^1.8.0"
  }
}
