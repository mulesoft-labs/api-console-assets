{
  "name": "api-console-assets",
  "description": "This repo only exists to publish api console components to npm",
  "main": "index.js",
  "scripts": {
    "build": "bower update && find ./bower_components -name package.json -exec rm {} \\; && node ./create-bundle.js",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mulesoft-labs/api-console-assets.git"
  },
  "author": "Mulesoft",
  "devDependencies": {
    "api-console-builder": "^0.4.2",
    "bower": "^1.8.0",
    "semantic-release": "^6.3.6"
  }
}
